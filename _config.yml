# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: literi
email: 13569829700@163.com
description: >- # this means to ignore newlines until "baseurl:"
  Coding and learning

# 站点基础信息
baseurl: ""
url: ""
lang: zh-CN

# 使用自定义主题（Sass 构建）
theme: null
# 页面默认配置
# 设置全局默认布局
defaults:
  - scope:
      path: ""  # 空字符串表示匹配所有文件（根目录及所有子目录）
      type: "pages"  # 作用于所有“页面”类型文件（普通 md/html 文件）
    values:
      layout: "default"  # 默认使用 _layouts/default.html 布局

  # 单独为 _posts 目录的文章设置默认布局
  - scope:
      path: "_posts"  # 仅作用于 _posts 目录
      type: "posts"   # 作用于“文章”类型文件（_posts 下的博客文章）
    values:
      layout: "post"  # 默认使用 _layouts/post.html 布局

sass:
  # 指定 Sass 编译器（避免 GitHub Pages 兼容问题）
  style: compressed # 编译为压缩后的 CSS
  sass_dir: _sass    # 指定 Sass 源文件目录
  load_paths:
    - _sass  # 明确指定 Sass 加载路径

# Jekyll 基础配置
markdown: kramdown
kramdown:
  toc_levels: 2..5  # 全局默认只解析 h2-h4 生成目录
  auto_ids: true    # 自动为标题生成锚点（默认开启）
  entity_output: as_char  # 避免中文标题锚点乱码
  auto_ids_as_header: true
  syntax_highlighter: rouge  # 指定语法高亮器为 Rouge
  syntax_highlighter_opts:
    block:
      line_numbers: true  # 可选：显示行号

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/

